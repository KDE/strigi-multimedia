if include_ogg_SUBDIR
KFILE_OGG_SUBDIR=ogg
endif

if include_flac_SUBDIR
KFILE_FLAC_SUBDIR=flac
endif

if include_mp3_SUBDIR
KFILE_MP3_SUBDIR=mp3
endif

SUBDIRS=m3u $(KFILE_MP3_SUBDIR) au avi wav sid $(KFILE_OGG_SUBDIR) \
        $(KFILE_FLAC_SUBDIR)
